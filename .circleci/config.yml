version: 2.1
jobs:
    Docker:
        - image: cimg/python:3.11.3
    steps:
        - checkout
        - run:
            name: Python
            command: | 
              python --version

workflows:
    Test:
       jobs:
           - Docker
