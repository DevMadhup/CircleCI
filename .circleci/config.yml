    version: 2.1
    jobs:
        Version:
            docker:
                - image: cimg/python:3.11.3
            steps:
                - checkout
                - run: echo "Hello"
                - setup_remote_docker:
                    version: 20.10.14
                - run: 
                    name: Docker Version Check
                    command: docker --version
                - run:
                    name: Docker Image Build
                    command: |
                        docker build . -t todo_app --name python-django-app-image
    workflows:
            Test:
                jobs:
                    - Version
