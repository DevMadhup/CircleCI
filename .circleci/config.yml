    version: 2.1
    jobs:
        Version:
            docker:
                - image: cimg/python:3.11.3
            steps:
                - checkout
                - run: docker build . -t todoapp
                - run: 
                    name: Docker Version Check
                    command: |
                        docker --version
                       
              
    workflows:
            Test:
                jobs:
                    - Version
